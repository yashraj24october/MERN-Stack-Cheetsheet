In node js, we have http module for handling http request and response on nodejs app.

First we need to import the http module
That comes with handly methods to  create server, create APIs and handle http requests.


Lets understand step by step

Step 1: importing http module
	Const http = require('http');
	
Step 2: Creating a server with response

Const server = http.createServer((request,response)=>{

response.writeHead(<status_code>,{'Content-Type' : 'Application/json'});


response.end(Json.stringify({
'data' : "Hello"
})

});

to send the response in nodejs, we use response.end();


First we set Header of response using response.writeHead(<status_code>,{ 'Content-Type' : 'application/json'});

then after, we send the response using response.end()

to send the json object, first we need to content it to string using JSON.stringify()
