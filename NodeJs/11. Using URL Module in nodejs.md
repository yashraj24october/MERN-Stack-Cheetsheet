In nodejs, we can use url module to get informations about request urls and also many operation on url.


There are many helpful methods, we can use:

	1. url.parse(request.url)

-> it return many data from a request url like pathname, query params, etc.
